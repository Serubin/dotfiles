#!/bin/zsh
# Zsh jump-target setup
#

# Set theme settings (blue)
ZSH_JUMP_TARGET_STYLE="fg=blue,bold,underline"

# Load Jump Target
autoload -Uz jump-target
zle -N jump-target

# Bind key ^F
bindkey "^F" jump-target
