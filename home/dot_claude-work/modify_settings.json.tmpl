{{- /* One line on purpose: the merge script is shared by ~/.claude and both
       per-account dirs, so it lives in .chezmoitemplates/claude-settings-merge.py.
       Edit the FORCE/SEED constants there, not here. */ -}}
{{ includeTemplate "claude-settings-merge.py" . }}
