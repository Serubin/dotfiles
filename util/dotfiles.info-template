#!/bin/bash

# Dotfiles config verson
DOTFILE_CONF_VERSION=1.2

# These will get filled in automaticly
DOTFILES_DIR="%location%"
DOTFILES_SHELL="%shell%"
DOTFILES_INSTALLED=""
DOTFILES_INSTALLED_LEN=""
git_name="%git-name%"
git_email="%git-email%"

TMUX_AS_SHELL="" # Use tmux as shell

# "Secret" Settings
PROMPT_UCFUSER="" # Uppercase first letter of username

#Theme Path
DOTFILES_THEME_PATH="${DOTFILES_DIR}/packages/shell/common/theme/default.theme"
